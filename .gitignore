# Build output
/bin/
*.o
*.exe

# Test and coverage output
coverage.out
coverage.html
/pkg/infrastructure/mqtt/packet/testdata/fuzz/

# Benchmark result JSON. RESULTS.md is committed; the raw runs are not.
/bench/results/

# Local-only module overlay used when building without access to the Go module
# proxy. Not part of the project.
go.local.mod
go.local.sum

# Editors
.idea/
.vscode/
*.swp
.DS_Store

# Local secrets and environment overrides
.env
config/config.*.local.yaml
